# Expense-Sharing-System
 a full-stack web application that allows a group of people to track shared expenses and automatically calculate who owes whom. The system must use React for frontend, Node.js with Express for backend, and file system (fs) for JSON-based persistence.
